# 📋 Configuração do Vercel - Resumo Rápido

## 🎯 Informações para Preencher no Dashboard do Vercel

Use estas informações exatas ao criar o projeto no Vercel:

### 1. **Project Name** (Nome do Projeto)
```
aiexx-genai-text-to-3d-web
```

### 2. **Framework Preset**
```
Other
```
ou deixe em **branco** (Vercel detectará automaticamente)

### 3. **Root Directory** ⚠️ IMPORTANTE
```
web
```
**Clique em "Edit" e configure como `web`**

### 4. **Build Command**
```
npm install && npm run build
```

### 5. **Output Directory**
```
dist
```

### 6. **Install Command**
```
npm install
```

> **Nota**: O campo "Development Command" não está disponível na interface inicial do Vercel. Ele será configurado automaticamente pelo arquivo `web/vercel.json` se necessário.

---

## 📸 Visualização das Configurações

```
┌─────────────────────────────────────────┐
│  Project Name                           │
│  ┌───────────────────────────────────┐  │
│  │ aiexx-genai-text-to-3d-web       │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Framework Preset                      │
│  ┌───────────────────────────────────┐  │
│  │ Other                             │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Root Directory                         │
│  ┌───────────────────────────────────┐  │
│  │ web                    [Edit]     │  │ ← Clique aqui!
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Build Command                          │
│  ┌───────────────────────────────────┐  │
│  │ npm install && npm run build      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Output Directory                       │
│  ┌───────────────────────────────────┐  │
│  │ dist                              │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Install Command                        │
│  ┌───────────────────────────────────┐  │
│  │ npm install                       │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## ✅ Checklist de Configuração

- [ ] **Project Name**: `aiexx-genai-text-to-3d-web`
- [ ] **Framework Preset**: `Other` (ou deixar em branco)
- [ ] **Root Directory**: `web` ⚠️ **CRÍTICO**
- [ ] **Build Command**: `npm install && npm run build`
- [ ] **Output Directory**: `dist`
- [ ] **Install Command**: `npm install`

---

## 🔧 Configurações Adicionais (Opcional)

### Environment Variables ⚠️ SEGURANÇA

**IMPORTANTE**: Como o repositório é público, NUNCA adicione valores reais aqui diretamente. Use apenas variáveis de ambiente no Vercel Dashboard.

Consulte [.github/VERCEL_SECURITY.md](.github/VERCEL_SECURITY.md) para instruções detalhadas sobre segurança.

### Production Branch

Configure como: `master`

### Preview Branches

Deixe habilitado para previews automáticos de PRs.

---

## 📚 Documentação Completa

Para mais detalhes, consulte: [.github/VERCEL_SETUP.md](.github/VERCEL_SETUP.md)

---

**💡 Dica**: O arquivo `web/vercel.json` já contém todas as configurações. O Vercel detectará automaticamente este arquivo após a configuração inicial.

